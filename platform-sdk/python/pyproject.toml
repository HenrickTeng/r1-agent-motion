[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "r1-motion-platform-sdk"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["httpx>=0.28,<1"]

[tool.setuptools.packages.find]
where = ["src"]
